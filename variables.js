// Variables
/* 
1. Create a variable.js file and declare variables and assign string, boolean, 
undefined and null data types, Display all the value with there type.
*/
let myName = "Pujari Basvaraj" // String
var isValue =  true; // boolean
var isValid; // undefined
var isNull = null;// null

console.log(`value is ${myName} and type is ${typeof myName}`);
console.log(`value is ${isValue} and type is ${typeof isValue}`);
console.log(`value is ${isValid} and type is ${typeof isValid}`);
console.log(`value is ${isNull} and type is ${typeof isNull}`);